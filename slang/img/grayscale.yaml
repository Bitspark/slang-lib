in:
  type: map
  map:
    red:
      type: number
    green:
      type: number
    blue:
      type: number
    alpha:
      type: number
out:
  type: map
  map:
    red:
      type: number
    green:
      type: number
    blue:
      type: number
    alpha:
      type: number

operators:
  gray:
    operator: slang.eval
    generics:
      paramsMap:
        type: map
        map:
          red:
            type: number
          green:
            type: number
          blue:
            type: number
          alpha:
            type: number
    properties:
      expression: (0\.21*red+0\.72*green+0\.07*blue)/3

connections:
  (:
  - (gray
  alpha(:
  - )alpha
  gray):
  - )red
  - )green
  - )blue
