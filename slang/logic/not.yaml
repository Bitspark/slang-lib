services:
  main:
    in:
      type: boolean
    out:
      type: boolean
operators:
  eval:
    operator: slang.eval
    properties:
      expression: '!x'
      variables:
      - x
connections:
  (:
  - x(eval
  eval):
  - )
