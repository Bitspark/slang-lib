services:
  main:
    in:
      type: map
      map:
        a:
          type: number
        b:
          type: number
    out:
      type: boolean
operators:
  eq:
    operator: slang.cmp.eq
  gt:
    operator: slang.cmp.gt
  or:
    operator: slang.logic.or
connections:
  (:
  - (eq
  - (gt
  eq):
  - b(or
  gt):
  - a(or
  or):
  - )
