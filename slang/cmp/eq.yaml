services:
  main:
    in:
      type: map
      map:
        a:
          type: primitive
        b:
          type: primitive
    out:
      type: boolean
operators:
  eval:
    operator: slang.eval
    properties:
      expression: a == b
      variables:
      - a
      - b
connections:
  (:
  - (eval
  eval):
  - )
