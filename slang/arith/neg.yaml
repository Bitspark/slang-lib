services:
  main:
    in:
      type: number
    out:
      type: number
operators:
  eval:
    operator: slang.eval
    properties:
      expression: -x
      variables:
      - x
connections:
  (:
  - x(eval
  eval):
  - )
