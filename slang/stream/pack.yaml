services:
  main:
    in:
      type: generic
      generic: itemType
    out:
      type: stream
      stream:
        type: generic
        generic: itemType
operators:
  "false":
    operator: slang.const
    properties:
      value: false
    generics:
      valueType:
        type: boolean
  loop:
    operator: slang.loop
    generics:
      itemType:
        type: generic
        generic: itemType
      stateType:
        type: generic
        generic: itemType
connections:
  (:
  - (loop
  false):
  - ~.continue(loop.iteration
  loop.iteration):
  - )
  loop.iteration)~:
  - (false
  - ~.state(loop.iteration
