services:
  main:
    in:
      type: map
      map:
        triggers:
          type: stream
          stream:
            type: trigger
        value:
          type: generic
          generic: itemType
    out:
      type: stream
      stream:
        type: generic
        generic: itemType
operators:
  repeater:
    operator: slang.aggregate
    generics:
      itemType:
        type: trigger
      stateType:
        type: generic
        generic: itemType
connections:
  repeater.iteration)~.state:
  - ~(repeater.iteration
  - )~
  triggers(:
  - items(repeater
  value(:
  - init(repeater
