# Pack number: Wraps around pack with number generic
---
in:
  type: number
out:
  type: stream
  stream:
    type: number

operators:
  # Packer
  packer:
    operator: .pack
    generics:
      itemType:
        type: number

connections:
  (:
  - (packer
  packer):
  - )
