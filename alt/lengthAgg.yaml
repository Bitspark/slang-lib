services:
  main:
    in:
      type: stream
      stream:
        type: generic
        generic: itemType
    out:
      type: number
operators:
  addOne:
    operator: slang.eval
    properties:
      expression: x+1
      variables:
      - x
  counter:
    operator: slang.aggregate
    generics:
      itemType:
        type: generic
        generic: itemType
      stateType:
        type: number
  zero:
    operator: slang.const
    properties:
      value: 0
    generics:
      valueType:
        type: number
connections:
  (:
  - items(counter
  - (zero
  addOne):
  - ~(counter.iteration
  counter):
  - )
  counter.iteration)~.state:
  - x(addOne
  zero):
  - init(counter
